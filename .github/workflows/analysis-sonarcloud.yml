name: Analysis - SonarCloud

on:
  push:
    branches:
      - main
  pull_request:

env:
  CMAKE_BUILD_PARALLEL_LEVEL: 2

jobs:
  sonarcloud:
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install Linux dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential cmake ninja-build ccache \
            libharfbuzz-dev libfribidi-dev libprotobuf-dev protobuf-compiler \
            libphysfs-dev nlohmann-json3-dev libpugixml-dev libfmt-dev \
            libopenal-dev libvorbis-dev libglew-dev libluajit-5.1-dev

      - name: Cache CCache
        uses: actions/cache@v4
        with:
          path: $HOME/.ccache
          key: ccache-${{ runner.os }}-${{ hashFiles('CMakeLists.txt', 'vcpkg.json', 'src/**/*.cpp', 'src/**/*.h') }}
          restore-keys: |
            ccache-${{ runner.os }}-

      - name: Cache SonarCloud analysis data
        uses: actions/cache@v4
        with:
          path: $HOME/.cfamily
          key: sonar-cfamily-${{ runner.os }}-${{ hashFiles('CMakeLists.txt', 'vcpkg.json', 'src/**/*.cpp', 'src/**/*.h') }}
          restore-keys: |
            sonar-cfamily-${{ runner.os }}-

      - name: Cache SonarCloud packages
        uses: actions/cache@v4
        with:
          path: $HOME/.sonar/cache
          key: sonar-packages-${{ runner.os }}
          restore-keys: |
            sonar-packages-${{ runner.os }}-

      - name: Extract vcpkg commit ID
        id: vcpkg-step
        run: |
          vcpkgCommitId=$(grep '"builtin-baseline"' vcpkg.json | awk -F: '{print $2}' | tr -d '," ')
          echo "vcpkgGitCommitId=$vcpkgCommitId" >> "$GITHUB_OUTPUT"

      - name: Cache vcpkg installed packages
        uses: actions/cache@v4
        with:
          path: build/vcpkg_installed
          key: vcpkg-installed-${{ runner.os }}-${{ steps.vcpkg-step.outputs.vcpkgGitCommitId }}
          restore-keys: |
            vcpkg-installed-${{ runner.os }}-

      - name: Setup vcpkg with baseline
        uses: lukka/run-vcpkg@v11
        with:
          vcpkgGitCommitId: ${{ steps.vcpkg-step.outputs.vcpkgGitCommitId }}
          # Jednoznaczne wskazanie pliku manifestu
          vcpkgJsonGlob: vcpkg.json
          vcpkgConfigurationJsonGlob: vcpkg-configuration.json

      - name: Build & Generate compilation database
        run: |
          cmake -S . -B build \
            -GNinja \
            -DCMAKE_EXPORT_COMPILE_COMMANDS=ON \
            -DCMAKE_TOOLCHAIN_FILE="$VCPKG_ROOT/scripts/buildsystems/vcpkg.cmake" \
            -DCMAKE_BUILD_TYPE=Debug \
            -DOTC_ENABLE_TTF=ON -DOTC_ENABLE_HARFBUZZ=ON -DOTC_ENABLE_FRIBIDI=ON \
            -DOPTIONS_ENABLE_CCACHE=ON \
            -DSPEED_UP_BUILD_UNITY=OFF
          cmake --build build --parallel
          # przenosimy compile_commands.json do katalogu głównego dla SonarCloud
          cp build/compile_commands.json .

      - name: SonarCloud Scan
        uses: sonarsource/sonarcloud-github-action@v2
        with:
          args: >
            -Dsonar.projectKey=PtakuPL_testyy
            -Dsonar.organization=ptakupl
            -Dsonar.sources=src
            -Dsonar.cfamily.compile-commands=compile_commands.json
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
